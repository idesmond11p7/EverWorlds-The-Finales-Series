# OPAQUE Archived

OPAQUE is intentionally **inactive** for the current EverWorlds build as of 2026-09-25.

The complete OPAQUE document set has been preserved unchanged on the Git branch:

`archive/opaque-2026-09-25`

That branch is the historical archive. The active project root is intentionally free of OPAQUE documents so current EverWorlds work can proceed from raw, unstructured ideas and direct experimentation.

This is an archive, not a deletion and not a rejection of the work. Do not treat archived OPAQUE material as active project requirements or implementation instructions unless explicitly reactivated later.
